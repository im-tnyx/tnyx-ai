# TNYX Mobile (Flutter Base)

This module contains the Flutter foundation for the TNYX mobile app.

Current scope:

- Theme tokens (colors, typography, fonts, dimens)
- Welcome feature base flow:
  - Route -> Screen -> ViewModel -> UiState + Action
- App router with welcome as initial route
